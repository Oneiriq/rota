edition = "2021"
max_width = 100
hard_tabs = false
tab_spaces = 2
newline_style = "Unix"
reorder_imports = true
use_field_init_shorthand = true
use_try_shorthand = true
# imports_granularity = "Module" + group_imports = "StdExternalCrate" are
# nightly-only — re-enable once those options stabilize on the pinned
# toolchain (1.88.0 stable as of writing).
